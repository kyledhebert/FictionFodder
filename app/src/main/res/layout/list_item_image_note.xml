<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="16dp"
                android:orientation="vertical">

    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:id="@+id/image_note_thumbnail_image_view"
        android:layout_alignParentEnd="true"
        android:contentDescription="@string/image_note_thumbnail_content_description"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/image_note_caption_text_view"
        android:padding="4dp"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@id/image_note_thumbnail_image_view"
        android:textStyle="bold"
        android:textSize="20sp"
        tools:text="Image Caption"
        />



</RelativeLayout>